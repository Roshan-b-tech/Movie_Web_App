{
    "buildCommand": "cd react_learning && npm install && npm run build",
    "outputDirectory": "react_learning/dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}